services:
  - type: web
    name: team-calendar
    env: node
    buildCommand: npm install && npm run build
    startCommand: cd dist && node index.js
    envVars:
      - key: NODE_ENV
        value: production